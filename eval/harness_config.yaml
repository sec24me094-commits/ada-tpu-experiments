# lm-evaluation-harness configuration.
# Status: template — the model wrapper this points at doesn't exist yet
# (see README.md). Fill in once ADAModel.from_pretrained + the harness
# wrapper are implemented.

model: ada_wrapper
model_args:
  checkpoint: null    # [FILL: path to checkpoint once trained]
  config: null         # [FILL: path to configs/*.yaml matching the checkpoint]

tasks:
  - lambada_openai
  - hellaswag
  - arc_easy
  - arc_challenge
  - winogrande

num_fewshot: 0   # zero-shot, per the research proposal's evaluation plan
batch_size: auto
output_path: eval/results/
