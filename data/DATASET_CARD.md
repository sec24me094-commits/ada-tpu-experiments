# Dataset Card — [FILL: dataset name]

Status: template only — no dataset has been selected/processed yet.

## Source
[FILL: e.g. SlimPajama subset, The Pile subset, or a custom corpus — name,
license, and URL/citation]

## Preprocessing
- Filtering: [FILL — quality filter rules used by `preprocess/filter.py`]
- Deduplication: [FILL — near-duplicate removal method/threshold used by
  `preprocess/deduplicate.py`]
- Tokenizer: [FILL — name/path, vocab size (must match `ADAConfig.vocab_size`)]
- Packing: [FILL — sequence length, padding/truncation policy used by
  `preprocess/pack.py`]

## Statistics
See `stats/dataset_statistics.md` (populated after preprocessing runs).

## Splits
[FILL: train/validation/test split sizes and how they were created]

## Known limitations
[FILL: language coverage, potential biases, license restrictions on
redistribution of the processed dataset if any]
